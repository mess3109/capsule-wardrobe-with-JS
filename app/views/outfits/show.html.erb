<%= render 'outfit' %>

<% if !@items.empty? %>
	<p>Clothing in Outfit:</p>

	<ul>
	<% @items.each do |item| %>
		<li> <%= link_to item.title, outfit_item_path(@outfit, item) %> - <%= item.category.title %> - <%= link_to "Remove from Outfit", { :controller => :item_outfits, :action => :destroy, :item => item, :outfit => @outfit }, :method => :delete %></li>
	<% end %>
<% end %>

</ul>

<% if !@all_user_items.empty? %>
	<p>Add clothing to outfit:</p>

	<% @all_user_items.each do |item| %>
	   <%= link_to item.title, item_path(item) %> | <%= link_to 'Add to Outfit', {:controller => :item_outfits, :action => :create, :item_id => item.id, :outfit_id => @outfit.id}, :method => :post %><br>
	<% end %>
<% end %>

<p>
<%= link_to "Outfits", outfits_path %> | 
<%= link_to "Edit", edit_outfit_path %> | 
<%= link_to "Delete", { :controller => :outfits, :action => :destroy} , :method => :delete %> 
</p>