<h4>Wardrobe</h4>

<ul>
<% current_user.items_sorted_by_category.each do |item| %>
	  <li> <%= link_to item.title.titleize, item_path(item) %> - <%= item.category.title.titleize %></li>
<% end %>
</ul>
